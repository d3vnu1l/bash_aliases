#!/usr/bin/env bash

alias bt="btop"
alias cpr="cp -r"
alias dfh="df -h"
alias fdl="fdisk -l"
alias fr="free -h"
alias ll="ls -lah"
alias llg="ls -lah | grep"
alias ra="ranger"

alias punused="pacman -Rns $(pacman -Qdtq)"

# These entries ensure aliases are expanded before using aliases as a subcommand of another command.
alias sudo="sudo "
alias watch="watch "
