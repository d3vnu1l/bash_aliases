#!/usr/bin/env bash

alias bt="btop"
alias cpr="cp -r"
alias dfh="df -h"
alias fdl="fdisk -l"
alias fr="free -h"
alias ll="ls -lah"
alias ra="ranger"
alias sudo="sudo " # Support using aliases with sudo
