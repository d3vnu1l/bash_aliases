#!/usr/bin/env bash

alias ga="git add"
alias gaa="git add --all"
alias gap="git add -p"
alias gcm="git commit -m"
alias gp="git push"
alias gs="git status"
alias gsuir="git submodule update --init --recursive"